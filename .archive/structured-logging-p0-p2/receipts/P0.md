# P0 — 契约、覆盖与受控基线执行回执

导航：[执行入口](../../../.scratch/structured-logging/stage-prompts.md) · [进度表](../../../.scratch/structured-logging/progress.md)。

## 本次范围和代码状态

用户指定P0–P1；本回执对应06。沿用 refactor/issue2-260831-153007，基于3fa218f，
当前工作区未提交。P0新增 contract-v1、baseline-budget、覆盖清单及独立crate的baseline
example；不改应用入口。契约/覆盖/预算版本均为v1。交付complete，验收passed，观察not-required。

## 前置与交付

无前置。核对三处Rust初始化、CLI命令、Python下载/上传及无独立subscriber的登录helpers，
HTTP-FLV/HLS与外部下载选择器；关键身份/nullable/枚举/旧源映射见覆盖清单。
旧扫描器虽保留64KiB尾部，但read_until可暂存无限长行；P1诊断捕获另设行硬限，不改旧实现。

## 可复现验证

同一机器、release、每组20,000合成事件；旧formatter输出到sink（控制台）+TempDir真实文件。

| 命令（cargo run -p biliup-observability --release --example baseline --） | P99 µs | emit / flush总ms | 文件bytes | dropped |
| --- | --- | --- | --- | --- |
| rust | 0.916 | 14 / 14 | 0 | 0 |
| wheel | 5.083 | 52 / 65 | 3578146 | 0 |
| python（下载形态） | 4.625 | 49 / 58 | 2258146 | 0 |
| python（上传形态） | 4.750 | 49 / 57 | 2258146 | 0 |

`/usr/bin/time -l target/release/examples/baseline wheel`额外测得峰值RSS 8,028,160 bytes，
P99 1.834µs，emit/flush 32/44ms，0丢弃。重复运行的波动保留，不择优替换第一组。
命令与数字来自本地合成程序，不含真实业务标识/数据。控制台终端渲染延迟不在口径内；
不跨日，不把never测试当作daily轮转演练；Rust/wheel/Python宿主集成均留给P2。

## 观察与回退边界

无长观察要求；没有启动真实录制、访问生产或双写。10秒受控新底座性能/调度干扰、故障
和容量预算由P1验证；P3/P4真实长观察仍未开始。仅合成载体场景，不声称业务覆盖。
旧源码与行为未改，无运行时回退开关可操作；去除未接入crate不影响旧服务。

## 判定与交接

P0通过：契约、全关键事实/已知空缺、采样前冻结的数字预算、受控旧基线和场景均存在。
P1可依赖上述v1；只在当前工作区拥有。下一步07实现独立底座，随后08；不允许直接旁路接入。
继续提示：`@.scratch/structured-logging/stage-prompts.md 实现 P1`。
