# P1 — 独立通用日志底座执行回执

导航：[执行入口](../stage-prompts.md) · [进度表](../progress.md)。

## 本次范围和代码状态

- 用户指定P0–P1；本回执对应07–08，无扩大范围。P2及业务迁移未开始。
- 源码基于3fa218f，分支refactor/issue2-260831-153007，**当前工作区未提交**。
  新增 `crates/biliup-observability/`，仅改Cargo成员/锁文件、effort文档、CODE_INDEX。
  `git diff --name-only -- crates/biliup crates/biliup-cli crates/stream-gears app` 为空。
- 本次交付complete、验收passed、观察not-required、阶段passed；契约/覆盖/预算均v1。
  没有发布、部署、访问生产或新增定时任务，不归档未完成的整个effort。

## 上阶段与依赖核验

| 前置 | 产物/门槛 | 本次核验 | 结论 |
| --- | --- | --- | --- |
| P0/06 | 契约、覆盖、采样前数字预算、实测旧基线 | contract-v1、coverage-ledger、baseline-budget、[P0](P0.md)；四种旧formatter形态20,000条实测 | passed，可由独立组件依赖 |
| 07→08 | 无server/业务库的发射/快照/队列/消费者 | core.rs内存Consumer测试；cargo tree仅通用库；SQLite实现Consumer | passed，不靠业务组件初始化 |

## 逐项交付与取舍

| 任务 | 实际交付 | Implementation / 验收 |
| --- | --- | --- |
| 07 | model/sanitize/diagnostic：受控标量、明确身份、中文摘要、有界脱敏、流式附件；capture：span创建/更新/嵌套快照、独立过滤、native/bridge；runtime：懒发射、队列双限/高等级预留、限时关闭、替换Consumer与独立health | complete / passed |
| 08 | 独立migrations/单连接writer，UID幂等、事件+附件事务、commit后高水位、只读分页/关联/时间查询；预算/低水位/清理/WAL保护、SQLite VM读超时、一致性备份、异常重开标记 | complete / passed |

单写连接替代“写连接池”，符合单writer要求；查询池独立max2。维护每类64条，比P0的≤256更严。
SQLite application_id拒绝外来/业务库，并验证拒绝前不改journal_mode。空间探针仅Unix；
无法探测时保守拒写，Windows尚未验证，不隐瞒为全平台通过。
恢复的损失摘要由独立Health（累计分级dropped+recoveries）回答；不靠故障库自记日志，
仅限频stderr告警。Health计数重启不保留，异常重开持久记录unclean_shutdowns，具体损失未知。
不承诺跨库事务/掉电零丢失；不会用新日志替换durable业务审计。

## 可复现验证

以下命令确已执行；最终源码的release检查包含1个单元、8个core、9个storage测试（其中
kill_child是父测试调用的辅助入口，单独无环境变量时不制造副作用）。

| 检查 | 命令 / 测试位置 | 结果与限制 |
| --- | --- | --- |
| 调试模式回归 | `cargo test -p biliup-observability` | 通过；后续首致命脱敏小修由最终release安全测试重验 |
| 最终release回归 | `cargo test -p biliup-observability --release` | 18测试通过；未运行业务全仓测试 |
| 严格lint / 格式 | `cargo clippy -p biliup-observability --all-targets -- -D warnings`；`cargo fmt -p biliup-observability -- --check` | 通过 |
| 导航 / diff | `python3 scripts/check_code_index.py`；`git diff --check` | 通过，70文件/39关系 |
| 依赖独立性 | `cargo tree -p biliup-observability --depth 1` | 无biliup-cli/Web/业务库依赖；已有依赖版本未升级 |
| 快照/传播 | core.rs：延迟record、父子/事件覆盖、双路异步+blocking+channel+callback、不同session与attempt、完整合成decision链 | 通过，不串台；不代表业务原生路径已接入 |
| 安全/过滤 | core.rs：未知Debug禁止执行、off不格式化、旧ERROR不挡新INFO、原生不入旧sink、超长错误、敏感键/URL/控制符、chunk边界/长行/首fatal | 通过，桥接同样脱敏；无标签秘密仍依赖调用者不传入 |
| 队列与失败 | core.rs：条数/字节预留、重要事件、失效Consumer三次后计损、恢复、阻塞Consumer限时关闭 | 通过；超时in_flight保持未知，不能假定后台被强制终止 |
| SQLite故障 | storage.rs：BEGIN IMMEDIATE忙锁、只读连接、不存在父目录、max_page_count满页、低盘保护、读事务pin住WAL | 通过；全部TempDir隔离，没有填满宿主盘或操作生产 |
| 提交/附件/保留 | storage.rs：重复UID、重启、游标无重漏、关联查询、附件列表分离、7天附件清理、超预算原子回滚、时间/容量清理与gap | 通过，日志删行不改变业务；不声称文件立即缩容 |
| 备份/恢复 | storage.rs：VACUUM INTO在writer打开时导出，源/备份只读结果一致，拒绝覆写目标 | 通过；无在线主库文件拷贝或业务库迁移 |
| 强杀 | storage.rs：父进程创建独立测试子进程，已提交一条，写锁阻止另一条提交，再kill/wait | 重启只看到已提交+重启事件，unclean窗口可见；未提交事件不可恢复 |
| 长读中断 | sqlite.rs：SQLite递归长查询VM deadline + pool复用/只读写入拒绝 | 通过，Rust超时以外还中断SQLite执行 |

首轮编译发现测试断言需Commit Debug，首次strict lint发现风格/跨平台cast告警，均修复并
重跑通过；未把失败命令当通过。随后加固外来库识别、维护WAL保护、SQLite VM超时、query输入
长度和首致命脱敏，并重跑最终release+负载；下面第一轮性能仅保留历史，不代表最终配置。

## 资源预算与观察

实际执行：`cargo build -p biliup-observability --release --example acceptance`，随后
`/usr/bin/time -l target/release/examples/acceptance data/observability-evidence/P1-final.json`。
先测无日志1ms周期10秒，再在相同调度器上测2,000事件/秒10秒；全部合成数据。最终报告在
已确认gitignore的data目录，公开只记资源指标，不复制实际运行标识/时间。

| 指标 | 最终实测 | P0门槛 |
| --- | --- | --- |
| 正常提交 | 20,000 / 20,000；0丢弃、0存储失败 | 全部提交、0未解释缺口 |
| 发射P99 | 38.125µs | ≤500µs |
| 无日志/有日志任务唤醒迟到P99 | 796.083 / 360.500µs | 增量≤10ms；负增量仅噪声，不主张加速 |
| 最后排空 | 112ms | ≤2秒 |
| 队列计费峰值 | 387,712 bytes | ≤16MiB且≤4096条 |
| 进程峰值RSS | 12,484,608 bytes | ≤128MiB |
| 20,000条分页查询P99 | 1,453.709µs | ≤250ms |
| 主库 / WAL（关闭前） | 21,467,136 / 1,631,552 bytes | 主库≤192MiB、WAL≤16MiB、合计≤208MiB |

首轮（加固前）同负载：P99 36.334µs、排空112ms、0丢弃；相关证据已由上述最终版本重验，
不能拿首轮替代最终结果。附件上限/过期、字节限额、满页、WAL pinned由独立故障用例补足，
不是在正常负载里伪造触发。此次未测跨日旧文件轮转、真实录制/上传、Python宿主集成或双源对比，
它们不是P1独立底座的通过证据，分别归属P2/P3。P3/P4连续7天且≥10场的观察未开始。

## 回退与阶段交接

- 旧入口/日志/过滤/页面未改，没有运行时开启新采集；移除独立crate无需业务库回滚。
  合成测试实际验证off懒发射/旧输出隔离、正常close及deadline；未演练生产开关回退。
- 所有指定交付/独立验证和v1预算通过，P1判定passed，无本阶段剩余阻塞。
  当前结果只存在本工作区；下一session须先核对产物及测试/配置差异，不把本回执视作别的checkout已具备。
- 下次第一步读取09及本回执，从现有三个初始化位置开始设计旁路组合；旧总过滤必须拆成
  per-layer过滤，不能新装第二个global subscriber。真实入口和原生业务覆盖仍待后续阶段。
- 可以开始P2，但本次没有执行。直接使用：`@.scratch/structured-logging/stage-prompts.md 实现 P2`。
