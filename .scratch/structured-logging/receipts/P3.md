# 阶段 P3 — 执行回执

导航：[执行入口](../stage-prompts.md) · [进度表](../progress.md)。
本阶段仍在进行中：下表逐项记录已完成与未完成，不把部分完成写成阶段通过。

## 本次范围和代码状态

- 用户指定阶段 / 限定任务：P3，未限定任务（12–16）。
- 实际处理任务：12（录制/分段/重连原生事件）。13–16 尚未开始，见「逐项交付」。
  另含两处小范围前置修正，原因与范围在下面单独说明。
- 代码所在分支：`refactor/issue2-260831-153007`；**当前工作区未提交**。
- 相关文件/差异范围：
  - `crates/biliup/src/downloader/util.rs`（分段身份、关闭回调、创建/关闭原生事件）
  - `crates/biliup/src/downloader/httpflv.rs`（DTS 汇总、断连原生事件、切片原因取值时机）
  - `crates/biliup-cli/src/observe.rs`（新增：录制身份与原生事件发射点）
  - `crates/biliup-cli/src/server/{common/download.rs,common/segment_enrollment.rs,
    core/downloader.rs,core/downloader/stream_gears.rs,infrastructure/{context,models}.rs}`
  - `crates/biliup-cli/migrations/22_add_segment_identity.sql`（加法迁移，可空）
  - `crates/stream-gears/src/lib.rs`（嵌入式下载入口的 task 身份）
  - `crates/biliup-observability/src/model.rs`（空 ID = 未知，不计 rejected）
  - `scripts/structured_logging/{recording_pilot.py,evidence.py,test_evidence.py}`
- 本次交付 / 验收 / 观察 / 阶段状态：in-progress / partial / pending / in-progress。
- 证据适用版本：contract-v1（本轮追加分段身份与空 ID 两条）、coverage-v1、evidence-v1；
  采集配置 `native_range=["recording"]`、bridge 开启、新旧过滤器均 info。

## 上阶段与依赖核验

| 前置阶段/任务 | 要求的产物或门槛 | 实际代码/文档/回执引用 | 本次检查或补验 | 结论与缺口 |
| --- | --- | --- | --- | --- |
| P2 / 09 入口旁路 | 新组件以可关闭旁路接入全部入口，旧输出不变 | `shadow.rs` 与三处入口初始化仍在 checkout；[P2](P2.md) | 复跑全工作区测试（含 shadow 11 项）；本轮新增原生事件后重新实跑 Python 入口 | 有效，未回退 |
| P2 / 10 证据导出 | 有界只读导出 + 确定性校验 | `scripts/structured_logging/evidence.py`；[P2](P2.md) | 用本阶段真实原生事件重新导出并校验，发现目录缺少汇总形态（见下） | 有效，已按需扩展 |
| P2 / 11 对比流程 | 三份提示词、视图隔离、差异闭环 | `reconcile.py` 与 P2 receipts | 本轮沿用其「差异→复现→修复→重采」流程处理 3 处差异 | 有效 |
| P1 / 07–08 底座 | 组件与独立 SQLite 可用 | `crates/biliup-observability/`；[P1](P1.md) | 21 项测试全绿；新增空 ID 用例 1 项 | 有效 |

## 本阶段逐项交付

| 任务 | 已实现内容 / 文件引用 | 仍缺的实现或验收 | Implementation | 验收结论 |
| --- | --- | --- | --- | --- |
| 12 录制试点 | 文件创建即分配 `segment_id`（`LifecycleFile::create`），随关闭回调、`SegmentInfo`、登记事务（迁移 22）传递；原生事件 `recording.started/stopped/segment_created/segment_closed/segment_enrolled/dts_backward/disconnected/retry_scheduled/reconnected`；DTS 首条 1:1 + 分段级汇总；重连只在连接真正建立时记 `recovered` | 服务端整场循环（`DownloadTask::execute`）的 started/stopped/retry_scheduled 仅有编译与代码级证据，缺真实开播样本；外部下载器（ffmpeg）与 HLS 路径未接入身份 | complete | partial |
| 13 上传等后处理 | 未开始 | 全部 | not-started | pending |
| 14 全范围覆盖 | 未开始 | 全部 | not-started | pending |
| 15 查询 API | 未开始 | 全部 | not-started | pending |
| 16 试用页面 | 未开始 | 全部 | not-started | pending |

关键取舍：

1. **不新建 span 传递身份。** `fmt` 层默认会把 span 字段打进旧行，新增 span 会改变旧输出、
   使 P2 的「旧输出逐字节不变」证据失效。改为契约里写明的显式字段载体：`RecordingOwner`
   随 `LifecycleFile` 走，`RecordingIdentity` 随录制循环走，每个原生事件自己带全身份。
2. **重连的判据是下一次连接真的建起来。** 退避决定时只把缺口存进 `ReconnectContext`，
   由 `StreamGears` 在帧头读通后才发 `recording.reconnected`，避免把「打算重连」记成「已恢复」。
3. **外部下载器保留待接入标记**（`segment_id: None`），不拿桥接文本冒充已迁移。

小范围前置修正（均记录在覆盖清单变更记录）：

- **切片关闭原因一直是 `Unknown`**：`parse_flv` 在 `set_size_position`/`set_start_time` 复位
  计数之后才读 `size_needed()/time_needed()`，因此配置切片的 `SegmentCloseReason` 恒为
  `Unknown`。本轮把取值移到复位之前并加回归测试。**这一处有业务可见副作用**：
  `download.rs` 依据 `TimedSplit|SizeSplit` 判定 `completed_configured_segment`，修复后线路
  健康才会把「完成了一个配置分段」计为稳定尝试；此前该信号一直为假。
- **导出器目录只认 DTS 的 1:1 形态**：汇总事件被判成缺字段。目录改为「多种合法形态取其一」，
  半个形态仍判缺失，并补回归用例。

## 可复现验证

| 检查/场景 | 实际命令或可复现步骤 | 使用的源码状态 | 实际结果 | 脱敏证据与限制 |
| --- | --- | --- | --- | --- |
| 全工作区单元/集成测试 | `cargo test --workspace` | 本轮工作区 | 通过：biliup 54、biliup-cli 322（6 ignored）+ 26 + 38 等，全部 0 失败 | 含本轮新增 5 项 |
| 分段身份与关闭事件 | `cargo test -p biliup --test recording_events` | 同上 | 2 项通过：两次创建身份互不相同、关闭沿用创建时身份、旧 `Save to` 行仍在自有 target | 合成文件，无网络 |
| 切片原因回归 | `cargo test -p biliup --lib split` | 同上 | 通过：合成 FLV 触发 ≥2 次配置切片，reason 为 `split_limit`，末段 `stream_end` | 修复前该断言必然失败 |
| DTS 汇总规则 | `cargo test -p biliup --lib dts_rollup` | 同上 | 通过：首条 1:1 无 count，汇总含 count=3/首末/极值，换分段另起记录 | 纯单元 |
| 服务端身份贯通 | `cargo test -p biliup-cli --lib server_recording_events` | 同上 | 通过：真实本地 HTTP + 真实 FLV 解析，`reconnected` 带 gap/DA，创建事件带房间/场次/DA，回调交出的 `segment_id` 均来自创建事件 | 无平台、无账号 |
| 身份持久化与不被改名 | `cargo test -p biliup-cli --test upload_reliability_incident target_12` | 同上 | 通过：登记写入 `segment_id`，重启后补扫提供不同 ID 时仍返回原身份 | 临时 sqlite |
| 空 ID 语义 | `cargo test -p biliup-observability` | 同上 | 21 项通过，含新增：空 ID 不入库也不计 rejected，格式非法仍计 1 | 合成 |
| 受控录制演练 | `python3 scripts/structured_logging/recording_pilot.py <私有目录>` | 同上 + `cargo build -p stream-gears` | 通过：3 次完整录制（其中 2 次并发交错）、9 个分段、6 次 `split_limit`、末段 `stream_end`、1 次 `transport_error` 断连、注入的 3 次 DTS 全部原生可见；旧 `download.log` 照常写入 | 合成媒体、本地回环，无账号 |
| 证据导出与事实确认 | `evidence.py export` + `validate --expectations` | 同上 | `completeness=complete`、`status=passed`、`native_coverage=requires-fact-validation`，7 条预期事实全部 confirmed（C02/C03/C04/C05） | 期望只用非身份字段，身份链另由原始库核对 |
| 格式与静态检查 | `cargo fmt --all -- --check`、`cargo clippy --workspace --all-targets` | 同上 | 本轮触达文件全部干净；剩余 6 个 fmt 差异与 `biliup`/`danmaku` 的 clippy 告警在改动前已存在（`git stash` 对照确认） | 未顺手改无关文件 |

未执行：真实平台开播录制、服务端整场循环的运行样本、长观察窗口。

## 运行观察与双源核对

- 本阶段要求：每一小批采双源证据并关闭差异；全范围第一轮完整观察属于任务 14，尚未开始。
- 采集与观察：受控演练已实跑并留证据包；**全范围观察未开始**。
- 覆盖的入口/场景：Python 下载函数（真实入口）+ 服务端拉流执行器（集成测试内真实 HTTP）；
  两次完整过程、双路交错、连续切片、传输失败断连、人工构造 DTS 异常。
  仍缺：真实开播的整场循环、外部下载器、HLS、断流后真正重连成功的真实样本。
- 双源报告引用：受控演练包 `P3-recording-pilot`（私有证据目录），manifest 完整、校验通过。
- 差异处置：本轮 3 处差异全部闭环并重新采样——切片原因恒 `Unknown`（修复）、导出目录不认
  汇总形态（修复）、期望事实引用了会被匿名化的原始 ID（改为非身份字段表达）。
- 资源与预算：演练规模远小于 P0 预算，未做新的量化压测；沿用 P1/P2 结论。
- 失效的旧证据：P2 的入口矩阵证据仍然有效（旧输出与旁路开关未变）；但 P2 各包的
  `native_coverage=not-started` 仅适用于当时的源码版本，本阶段起原生事件已进入采集范围。

## 回退与运行边界

- 保持不变：旧调用点、旧文案、旧过滤器、旧文件与旧日志页；原生事件走 `biliup::event`
  target，旧 sink 按 P2 的 per-layer filter 排除该 target。
- 回退方式：关闭 `BILIUP_OBSERVABILITY` 即停止全部采集（原生与桥接一起停），业务不受影响；
  回退本批代码则回到旧行为，但迁移 22 的 `segment_id` 列保留（可空，旧程序可读写）。
- 回退是否演练：**未单独演练**。P2 的关闭态/新库不可用三态演练仍有效，本轮未重跑。
- 无法恢复的边界：分段身份只对本批代码之后创建的文件存在；历史文件与外部下载器产物
  为 `NULL`，不按文件名倒推。

## 阶段判定与交接

- 结论：**进行中**（in-progress）。任务 12 交付完成、验收 partial；13–16 未开始。
- 未满足的条件：13/14/15/16 全部未实现；全范围第一轮完整观察未开始；服务端整场循环
  与外部下载器缺真实样本。
- 尚未提交：本轮全部改动仍在工作区。
- 下次第一个动作：进入任务 13，从 `common/upload.rs` 的 `process_with_upload` 与
  `attempt_lease.rs` 接上 12 的 `segment_id`，先做「预处理决定/结果」这一小批，再按同一
  流程采双源证据。
- 可直接继续：`@.scratch/structured-logging/stage-prompts.md 继续 P3，从上次未完成处接着做`。
- 是否可以开始 P4：否，P3 远未完成。

## 后续补验记录

- 待补：服务端真实开播的整场事件链（`recording.started/stopped/retry_scheduled` 的运行样本）。
  按仓库约定应在 dev 环境实跑，本轮未做，不以集成测试代替。
