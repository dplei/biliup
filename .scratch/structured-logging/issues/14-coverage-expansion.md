# 14 — 补齐支持入口、其余业务与审计边界

Status: needs-triage
Blocked by: 13
Phase: P3
Implementation: in-progress

来源：[覆盖清单](../coverage-ledger.md)、[独立原始对照](../rollout-plan.md)。

## 交付与步骤

1. 按 06 的入口/下载路径表逐项补原生观测，覆盖外部工具和其余下载器；分批接入，不能
   把「桥接看到了文本」当作业务字段已完成迁移。
2. 补齐系统启动/退出、认证健康变化、关键人工操作与恢复审计。系统无主播上下文时
   使用进程身份；CLI 用 task 身份，嵌入式调用用宿主拥有的生命周期。
3. 核对已有 durable 审计：留在业务事务；需要展示的记录通过稳定 event_uid 幂等投影。
   如有必须可靠保存的事实尚无业务持久化，明确设计业务事务/outbox 的增量任务，不用
   通用日志尽力写入伪装为完成，也不删除既有恢复账本。
4. 检查每条路径的外部 stderr 有界采集/退出码/首个错误，以及 HTTP 请求/凭据脱敏，
   页面默认不展示第三方诊断洪流；桥接仍可显式查询。
5. 验证 Rust CLI、wheel CLI、Python 下载/上传函数的正常/错误/多次调用/退出案例；
   覆盖未走 server 的持久化 opt-in/关闭情况。
6. 完成适用关键覆盖项及未迁移诊断的分类清单：保留桥接、无需持久化或显式不支持须有
   理由。所有关键项必须原生/指定权威源可回答，再启动全范围第一轮完整观察。

## 验收

- 所有承诺支持的入口和业务域有独立证据，未运行不算通过。
- 全范围正常场景与异常矩阵通过，桥接未迁移部分不会掩盖关键原生缺口。
- 审计回放幂等且不会随普通日志清理消失；普通日志故障不改变任务恢复行为。
- 整体资源/隐私/只读比较符合基线，旧链仍保留；第一轮观察的报告和差异处置可审阅。

## 回退与非目标

失败的业务批次回到旧链 + 桥接，其余已验证批次可继续。若涉及增量业务审计字段，使用
兼容迁移并保留旧程序读写能力，不通过删除表/字段回退。
不扩展到未承诺的平台或全仓业务重构。

## Comments

本任务完成的是覆盖，不是文件切换；旧系统至少保留到 17 的后续观察结束。

- 第一批独立上传入口已实现并提交，验收 partial：Rust/wheel CLI（命令/配置/追加）和
  Python 上传的 task、输入序号、attempt 与投稿结论已接入；三态/重复调用/回退和双源
  确定性校验通过。远端成功链仍须补验，详见
  [P3 本批回执](../receipts/P3.md#任务-14-第一批独立上传入口)。
- 第一批收尾时本任务保持 in-progress，下一批计划处理页面整场上传并补齐独立入口的正常远端样本；
  其余下载路径、系统/认证/审计/诊断、分类清单及完整观察尚未完成，不切换页面或旧写入。
- 第二批页面整场上传已实现，HTTP 响应 task 贯通后台传输与投稿；Rust/wheel 三态、并发与
  回退通过，Rust 页面真实私密投稿通过并只读确认。采样暴露的 `ResponseData` 脱敏遗漏已作
  必要补修、重导；成功样本与最终脱敏构建的适用范围分开记录，不算最终版本长观察。
  详见 [P3 第二批回执](../receipts/P3.md#任务-14-第二批页面整场上传)。14 仍 in-progress / partial，
  下批可接外部下载器/HLS，首批独立入口远端成功/恢复补验和其余覆盖/观察继续待办。
- 第三批 HLS 已交付：复用分段身份并记录序列缺口/不连续/类型化失败；独立下载补 DA，
  服务执行器媒体后重连和取消原因经过回归。列表终止/序号零/小数时长与 Python 错误返回
  是本批受控采证据所需的前置修复。三下载入口三态、重复正常/错误、双源与回退通过；
  CLI 提取器输出为 fixture，不算在线平台提取。服务整链、真实 HLS 断连恢复和负载未验。
  见 [P3 第三批回执](../receipts/P3.md#任务-14-第三批hls-下载)。本任务保持 in-progress / partial；
  下一批仅 FFmpeg 外部分段与退出诊断，系统/认证/审计及全范围观察继续待办。
- 第四批服务端 FFmpeg 下载器已交付：内外分段接入分段身份，外部分段的创建/关闭是真实观测，
  内部分段只发关闭事件、不补造创建时刻；取消标记先于杀进程写入，退出诊断带 stage/exit_code
  与有界 stderr 附件，含 URL/凭据线索的行整值脱敏。为采证据做了四条必要前置修复：外部分段
  `-loglevel quiet`→`error`、删除内部分段收尾处必然 ENOENT 的重复改名、分段列表行按输出目录
  还原、单段收尾失败不再结束整场录制。媒体本地合成、直播源为测试内限速 HTTP 服务，
  不接触真实平台。新记录既有缺陷：秒级文件名会让同一秒关闭的两段同名并被覆盖。
  见 [P3 第四批回执](../receipts/P3.md#任务-14-第四批外部下载器ffmpeg)。本任务保持
  in-progress / partial；下一批做 C01 系统启停与 C11 认证健康，审计、预处理诊断、
  分类清单与全范围观察继续待办。
- 第五批入口生命周期与凭据健康已交付：一次运行 = 一个 `Invocation`，正常/错误/被取消分别记
  executed`shutdown`、failed`entry_failed`、unknown`entry_interrupted`；强杀不执行析构，
  缺结束事件即缺失、不补造。运行 task 与业务 task 是两个身份，不互相代用。
  `auth.health_changed` 只由 `cookie_health` 状态机的两次跃迁发出，`auth.operation_failed`
  覆盖每次被计数的失败并按既有分类器定型，错误文本只用于分类随即丢弃；去抖窗口内的重复
  失败不计数也不发事件。新增允许键 `command`（解析后的子命令固定词）。必要前置修复两条：
  `record_error` 抽出接受时钟参数的版本以便验证去抖与阈值；包装 CLI 结果改按 `Debug` 分类，
  否则 `error_stack::Report` 的 Display 只有顶层 context，会把连接失败错判成 `invalid_response`。
  真实 `biliup` 二进制两次实跑并只读回查通过，另有 8 项测试；全部离线、无账号、无外部网络。
  见 [P3 第五批回执](../receipts/P3.md#任务-14-第五批入口生命周期与凭据健康c01--c11)。
  本任务保持 in-progress / partial；wheel CLI、Python 下载/上传与登录辅助函数只做编译核对，
  未起 Python 进程实跑。下一批做 C12 审计投影与 C13 剩余诊断，之后是分类清单与全范围观察。
- 第六批 C12/C13 已交付：恢复审计表新增稳定 `event_uid`，业务行先持久化、事件视图可启动回放
  且按 UID 幂等；预处理 FFmpeg、hook 命令失败统一带退出码和有界脱敏附件，弹幕/封面/非命令
  hook 补稳定辅助失败事件，原错误文本和 stderr 输出口径保留。审计双重回放和非零扫描器有
  受控实跑，全量 Rust 回归通过；真实恢复操作、弹幕/封面/hook 故障和 Python/wheel 入口仍待验。
  详见 [P3 第六批回执](../receipts/P3.md#任务-14-第六批持久审计投影与剩余诊断c12--c13)。
  本任务仍 in-progress / partial；下一批做分类清单与剩余入口运行矩阵，门槛满足后才开始长观察。
- 第七批完成未迁移诊断分类清单及文件级漂移校验：62 个含 tracing 宏的运行时源码文件都有
  默认处置，4 项明确不支持边界写明理由。源码复核同时纠正旧口径：`core/live.rs` 会实际选择
  Streamlink 与 YtDlp/YtArchive，它们不是不可达分支；两者仍缺稳定分段/attempt 身份与有界命令
  失败附件。danmaku spawned recorder 的运行中失败也未穿透第六批外层 auxiliary 事件。
  三项记 `coverage_gap` 并继续留在 C03/C13 分母，详见
  [分类清单](../diagnostic-classification.md) 与
  [P3 第七批回执](../receipts/P3.md#任务-14-第七批未迁移诊断分类清单)。本任务仍
  in-progress / partial，不能开始长观察；下一批先补这些原生边界，再做剩余入口运行矩阵。
