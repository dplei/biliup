.idea
.vscode
.gitattributes
*.pyc
*.flv
*.mp4
*.part
*.webm
backup
venv./
.venv/
.run/
test/
pdm.lock
.pdm-python
config.*.*
config.yaml
!public/config.yaml
/configlog.ini
ds_update.log*
*.cookie
*.zip
*.exe
*.egg-info
dist/
build/
cover/
cache/
.cursor/

*.back
bili.cookie.json
archive.txt
/node_modules
watch_process.pid
biliup/**/public
*.xml
!package-lock.json
qrcode.png
*.log
/*.ts
/biliup/stream-gears/
data/
.next
out
tsconfig.tsbuildinfo
downloads/
target
_internal
.trae/
pnpm-lock.yaml
cookies.json
.augment/
.cunzhi-memory/
.serena/
.cargo/
logs/
CLAUDE.md
AGENTS.md
# .claude/ 整体不入库（settings.local.json 等是个人配置），但仓库级 skill 例外：
# 它与本仓库的子命令强绑定，必须随仓库一起版本管理。
# 写成 .claude/* 而非 .claude/ 才能让下面这条否定生效——被排除的是目录本身时，
# git 不会再看目录里的内容。
.claude/*
!.claude/skills/
# macOS
.DS_Store

# 个人部署链路文档：ACR 实例、账号、镜像 digest、服务器细节，只对仓库主人有用，
# 不进公开仓库（本地保留，内容照常写全）。
BUILD_AND_DEPLOY.md
DEPLOY_UPLOAD_INIT_RECOVERY.md
